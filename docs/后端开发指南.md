# 后端开发指南

本文档面向后端开发者，说明 `server/` 目录的架构、各模块职责、API 设计、数据存储与扩展方式。

---

## 一、技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | 18+ | 运行时 |
| Express | 4.19 | Web 框架 |
| openai SDK | 4.56 | 调用通义千问（OpenAI 兼容接口） |
| dotenv | 16.4 | 环境变量管理 |
| cors | 2.8 | 跨域支持 |

> 无数据库，全部用 JSON 文件存储（会话历史 + 知识库），零依赖、适合学习项目。

---

## 二、目录结构

```
server/
├── src/
│   ├── config/
│   │   └── index.js              # 配置（模型/key/温度/baseURL）
│   ├── prompts/
│   │   └── system.js              # System Prompt（AI 人设）
│   ├── services/
│   │   ├── ai.service.js          # 通义千问 API 封装（chat/流式）
│   │   └── embedding.service.js   # Embedding 向量化服务（RAG 用）
│   ├── agent/                     # Agent 相关
│   │   ├── tools.js               # 工具集 + JSON Schema 定义
│   │   └── agent.service.js       # Agent 循环编排（含 RAG 预检索注入）
│   ├── routes/                    # 路由
│   │   ├── chat.js                # 对话 + 快捷问题
│   │   ├── agent.js               # Agent SSE 路由
│   │   ├── knowledge.js           # 知识库 CRUD + 检索对比 + 向量化
│   │   └── history.js             # 会话历史
│   ├── data/                      # 存储层
│   │   ├── store.js               # 会话历史存储
│   │   ├── kbStore.js             # 知识库存储（含向量 + 语义检索）
│   │   ├── knowledge.js           # 快捷问题 + 场景配置
│   │   └── knowledge.json        # 知识库数据文件（含 embedding 向量，自动生成）
│   └── app.js                     # Express 应用入口
├── .env                           # 环境变量（不入库）
├── .env.example                   # 环境变量示例
└── package.json
```

---

## 三、核心架构

### 1. 请求处理链路

```
客户端请求
   ↓
app.js（Express 中间件：cors / json / 日志）
   ↓
路由分发
   ├─ /api/chat        → chat.js
   ├─ /api/agent       → agent.js
   ├─ /api/knowledge   → knowledge.js
   ├─ /api/prompts     → prompts.js
   └─ /api/history     → history.js
   ↓
业务层（services / agent / data）
   ↓
通义千问 API / 本地文件
```

### 2. 三种工作模式的后端实现

#### 对话模式（[chat.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/routes/chat.js)）

1. 接收 `{ message, sessionId, scenario }`
2. 从 store 读取会话历史；**长对话摘要**：未摘要的早期历史 ≥ 2 条时，调 `summarizeHistory` 生成/合并摘要（非流式 chat），写回 session
3. 组装 messages：system（按 scenario 从 promptStore 取，含变量插值）+ 摘要 system 消息（如有）+ 最近 6 条历史原文 + 当前问题
4. **调用模型前推送 `messages` SSE 事件**：最终 messages 数组（每条含 chars/估算 tokens）+ totalTokens + summary，供前端消息预览面板可视化
5. 调用 [ai.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/services/ai.service.js) 的 `streamChat`，SSE 推送 token
6. 收集完整回复，存入 history

#### Agent 模式（[agent.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/routes/agent.js) + [agent.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/agent/agent.service.js)）

1. 接收 `{ task, sessionId }`
2. 从 store 读取历史
3. **RAG 预检索**（非问候类查询）：调用 `searchEntriesSemantic(task)` 语义检索知识库，结果注入 system prompt。同时推送 `thinking` + `tool` 事件让前端展示检索步骤
4. 组装 messages（system[含 RAG 上下文] + 历史 + task）
5. 进入 Agent 循环（最多 `MAX_ITERATIONS = 8` 轮）：
   - 调用通义千问，传入 messages + tools 定义（`tool_choice: 'auto'`）
   - 若返回 `tool_calls` → 执行对应工具 → 结果作为 `role: 'tool'` 消息追加 → 继续循环
   - 若返回 `stop`（无 tool_calls）→ 最终答案按字符分片流式推送
6. 全程通过 SSE 推送事件：`session` / `thinking` / `tool` / `content` / `usage` / `done`（对话模式另有 `messages` 事件：模型调用前推送最终 messages 预览）

> **为什么有预检索**：qwen-turbo 对概念类问题常"自以为知道"不主动调检索工具，甚至编造"据知识库"。预检索保证 RAG 必然生效。详见 [AI学习指南](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/docs/AI学习指南.md) 第四章第 6 节"两种 RAG 策略"。

#### 知识库管理（[knowledge.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/routes/knowledge.js)）

标准 RESTful CRUD，直接调用 [kbStore.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/kbStore.js)。

### 3. 配置管理

[config/index.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/config/index.js) 从 `.env` 读取：

```javascript
export const config = {
  dashscope: {
    apiKey: process.env.DASHSCOPE_API_KEY,
    baseURL: 'https://dashscope.aliyuncs.com/compatible-mode/v1',
    model: process.env.QWEN_MODEL || 'qwen-turbo',
    temperature: Number(process.env.TEMPERATURE) || 0.7,
    maxTokens: Number(process.env.MAX_TOKENS) || 2048,
    embeddingModel: process.env.EMBEDDING_MODEL || 'text-embedding-v3', // RAG 向量化模型
  },
  port: 3001,
};
```

`.env` 文件（参考 [.env.example](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/.env.example)）：

```bash
DASHSCOPE_API_KEY=sk-xxxxxxxx
QWEN_MODEL=qwen-turbo
TEMPERATURE=0.7
EMBEDDING_MODEL=text-embedding-v3   # 可选，默认即此值
```

---

## 四、API 接口

### 对话接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/chat` | 对话（SSE 流式） |
| GET | `/api/chat/quick-questions` | 获取快捷问题 |

POST `/api/chat` 请求体：
```json
{ "message": "useEffect 怎么用", "sessionId": "s_xxx" }
```

### Agent 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/agent` | Agent 任务（SSE 流式） |

请求体：
```json
{ "task": "查 react 包的版本", "sessionId": "s_xxx" }
```

SSE 事件类型见 [AI学习指南](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/docs/AI学习指南.md) 第三章第 5 节。

### 知识库接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/knowledge` | 列出全部条目（剥离向量） |
| GET | `/api/knowledge/stats` | 向量化状态：`{ total, embedded, pending }` |
| GET | `/api/knowledge/search?q=&mode=&topK=` | 检索：mode=`keyword`/`semantic`/`compare` |
| GET | `/api/knowledge/:id` | 获取单条 |
| POST | `/api/knowledge` | 创建条目（自动向量化） |
| POST | `/api/knowledge/embeddings/:action` | 向量化：action=`ensure`(补齐)/`regenerate`(全量重生成) |
| PUT | `/api/knowledge/:id` | 更新条目（自动重新向量化） |
| DELETE | `/api/knowledge/:id` | 删除条目 |

> 具名路由（`/stats`、`/search`、`/embeddings/:action`）必须在 `/:id` 之前注册，否则被参数路由捕获。

条目结构（`embedding` 字段仅内部使用，API 返回时剥离）：
```json
{
  "id": "kb_xxx",
  "title": "Promise",
  "keywords": ["promise", "async"],
  "category": "异步",
  "content": "...",
  "embedding": [0.12, -0.34, ...],
  "createdAt": "...",
  "updatedAt": "..."
}
```

### Prompt 接口（Prompt 实验室）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/prompts` | 场景列表 `{ scenarios: [{id, name, contentLength, updatedAt}], vars }` |
| GET | `/api/prompts/:id` | 单个场景：`raw`（原文）+ `rendered`（变量插值后）+ `vars` |
| PUT | `/api/prompts/:id` | 更新名称/内容，写入 prompts.json，**下一次对话立即生效** |

存储在 [promptStore.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/promptStore.js) 管理的 `prompts.json`，按 scenario id 组织。`getSystemPrompt(scenario)`（在 [knowledge.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/knowledge.js)）优先读 prompts.json，读不到回退硬编码 system.js。

支持的模板变量：`{{datetime}}`、`{{date}}`、`{{time}}`、`{{weekday}}`，渲染时替换为当前时间。

### 会话历史接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/history` | 会话列表 |
| GET | `/api/history/:id` | 会话详情（含消息） |
| POST | `/api/history` | 新建会话 |
| DELETE | `/api/history/:id` | 删除会话 |

### 其他

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/` | 根路由（列出所有端点） |

---

## 五、数据存储

### 1. 会话历史（[store.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/store.js)）

存到 `server/history.json`：

```json
{
  "sessions": [
    {
      "id": "s_xxx",
      "title": "useEffect 用法",
      "createdAt": "...",
      "updatedAt": "...",
      "messages": [
        { "role": "user", "content": "..." },
        { "role": "assistant", "content": "..." }
      ]
    }
  ]
}
```

### 2. 知识库（[kbStore.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/kbStore.js)）

存到 `server/knowledge.json`，首次启动自动写入 6 条种子数据（虚拟DOM、闭包、事件循环、useEffect、SSR、Tree Shaking）。每条条目带 `embedding` 向量字段（1024 维）。

**两种检索**：
- `searchEntries(query)`：关键词匹配（title + keywords + content + category 任一命中）
- `searchEntriesSemantic(query, topK=3, threshold=0.3)`：语义检索，先 `ensureEmbeddings()` 懒补齐，再算余弦相似度取 Top-K

**向量化**：
- `createEntry` / `updateEntry`：保存时自动生成/重新生成向量
- `ensureEmbeddings()`：补齐缺失向量（首次语义检索时触发）
- `regenerateAllEmbeddings()`：全量重新生成（换模型时用）
- `stats()`：`{ total, embedded, pending }`

**降级**：未配置 `DASHSCOPE_API_KEY` 时向量化失败，`embedding` 置空，语义检索自动退化为关键词检索。

### 3. 快捷问题与场景（[knowledge.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/knowledge.js)）

```javascript
export const QUICK_QUESTIONS = [
  { id: 'q1', text: 'useEffect 和 useLayoutEffect 的区别？' },
  // ...
];
export const DEFAULT_SCENE = 'frontend_dev';
```

### 4. Prompt 场景（[promptStore.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/promptStore.js)）

存到 `src/data/prompts.json`，结构为 `{ scenarios: [{ id, name, content, updatedAt }] }`。首次请求时懒加载到内存，PUT 时同步写文件。`renderPrompt(content)` 做 `{{变量}}` 插值；`getSystemPrompt(scenario)` 优先读它，失败回退 system.js 硬编码。

---

## 六、Agent 工具扩展

### 添加新工具的三个步骤

以"查 GitHub star 数"为例：

#### 步骤 1：写工具函数

在 [tools.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/agent/tools.js) 中：

```javascript
async function getGithubStars(repoName) {
  try {
    const res = await fetch(`https://api.github.com/repos/${repoName}`);
    const data = await res.json();
    return `仓库 ${repoName} 有 ${data.stargazers_count} 个 star`;
  } catch (e) {
    return `查询失败：${e.message}`;
  }
}
```

#### 步骤 2：注册到 TOOLS 对象

```javascript
export const TOOLS = {
  search_frontend_kb: searchFrontendKb,
  get_npm_package_info: getNpmPackageInfo,
  analyze_code: analyzeCode,
  get_github_stars: getGithubStars,  // 新增
};
```

#### 步骤 3：添加 JSON Schema 定义

在 `tools` 数组中：

```javascript
{
  type: 'function',
  function: {
    name: 'get_github_stars',
    description: '查询 GitHub 仓库的 star 数量',
    parameters: {
      type: 'object',
      properties: {
        repoName: { type: 'string', description: '仓库名，如 facebook/react' }
      },
      required: ['repoName']
    }
  }
}
```

重启后端，Agent 即可自主决定是否调用新工具。

### 工具函数的注意事项

- **必须异步**（`async`），因为可能涉及网络请求
- **必须返回字符串**，模型据此理解执行结果
- **必须 try/catch**，失败时返回错误信息给模型（而不是抛异常中断 Agent）
- **参数从模型的 `tool_calls` 解析**，注意类型转换

---

## 六点五、RAG：Embedding 服务与预检索

### Embedding 服务（[embedding.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/services/embedding.service.js)）

复用通义千问 OpenAI 兼容端点，只是调 `/embeddings` 而非 `/chat/completions`：

```javascript
const res = await client.embeddings.create({
  model: config.dashscope.embeddingModel,  // text-embedding-v3
  input: ['文本1', '文本2'],  // 批量，返回多个向量
});
// res.data 按 index 排序，每项 .embedding 是 1024 维数组
```

导出：
- `embedTexts(texts)`：批量向量化
- `embedQuery(text)`：单条查询向量化
- `cosineSimilarity(a, b)`：余弦相似度（纯 JS，零依赖）
- `hasEmbeddingConfig()`：是否配了 API Key

### Agent 预检索注入（[agent.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/agent/agent.service.js)）

`runAgent` 在进入模型循环前，对非问候类查询先做语义检索：

```javascript
if (shouldForceKbRetrieve(task)) {
  const results = await searchEntriesSemantic(task, 3, 0.3);
  if (results.length > 0) {
    ragContext = formatSearchResultText(task, results);
    yield { type: 'thinking', step: 1, plan: [...] };  // 推送给前端展示
    yield { type: 'tool', step: 1, ... };
  }
}
// 注入 system prompt
const systemContent = ragContext
  ? `${AGENT_SYSTEM_PROMPT}\n\n## 已检索到的知识库内容...\n${ragContext}`
  : AGENT_SYSTEM_PROMPT;
```

> `shouldForceKbRetrieve(task)` 用简单正则排除纯问候（你好/谢谢等），其余都预检索。模型循环步骤编号用 `stepBase` 偏移，避免与预检索的 step 1 冲突。

---

## 七、SSE 流式实现

### 对话流式（[ai.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/services/ai.service.js)）

用 openai SDK 的 `stream: true`：

```javascript
const stream = await openai.chat.completions.create({
  model, messages, stream: true, temperature,
});
for await (const chunk of stream) {
  const content = chunk.choices[0]?.delta?.content;
  if (content) {
    res.write(`data: ${JSON.stringify({ content })}\n\n`);
  }
}
res.write('data: [DONE]\n\n');
res.end();
```

### Agent 流式（[agent.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/agent/agent.service.js)）

Agent 的流式分两类：

1. **工具调用阶段**：非流式，一次返回完整 `tool_calls`，后端执行后推送 `thinking` / `tool` 事件
2. **最终答案阶段**：流式，按字符分片推送 `content` 事件

### SSE 响应头

```javascript
res.writeHead(200, {
  'Content-Type': 'text/event-stream',
  'Cache-Control': 'no-cache',
  'Connection': 'keep-alive',
});
```

### 中断处理

正确做法：监听 `res.on('close')` + `finished` 守卫，仅在客户端真实断开时才中断模型调用。

```javascript
let finished = false;
res.on('close', () => {
  if (!finished) {
    // 客户端断开，中止模型调用
    controller.abort();
  }
});
// 完成后
finished = true;
res.end();
```

> 注意：不要用 `req.on('close')`，它在请求体读完后立即触发，会误中断刚发出的模型调用。

---

## 八、开发流程

### 启动

```bash
cd server
cp .env.example .env     # 首次，填入 DASHSCOPE_API_KEY
npm install               # 首次
npm run dev                # 开发模式（--watch 自动重启）
npm start                  # 生产模式（无 watch）
```

开发模式用 `node --watch`，修改 `src/` 下文件自动重启，无需 nodemon。

### 测试接口

```bash
# 健康检查
curl http://localhost:3001/api/health

# 知识库列表
curl http://localhost:3001/api/knowledge

# 向量化状态
curl http://localhost:3001/api/knowledge/stats

# 检索对比（关键词 vs 语义）
curl "http://localhost:3001/api/knowledge/search?q=函数怎么记住外部变量&mode=compare"

# 重新生成全部向量
curl -X POST http://localhost:3001/api/knowledge/embeddings/regenerate

# 对话（SSE）
curl -N -X POST http://localhost:3001/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"什么是闭包"}'

# Agent 任务（SSE，会触发 RAG 预检索）
curl -N -X POST http://localhost:3001/api/agent \
  -H "Content-Type: application/json" \
  -d '{"task":"查 react 包的版本"}'
```

---

## 九、常见改动点

### 1. 修改 AI 人设

**推荐**：在前端「🧪 Prompt 实验室」在线编辑，保存写入 prompts.json，下一次对话立即生效（无需重启）。

也可直接编辑 [system.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/prompts/system.js) 的硬编码默认值——它是 prompts.json 缺失/读取失败时的兜底。注意 prompts.json 中已存在同 id 场景时，硬编码不会生效。

### 2. 切换模型/温度

编辑 `.env`：
```bash
QWEN_MODEL=qwen-max
TEMPERATURE=0.3
```

### 3. 修改快捷问题

编辑 [knowledge.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/data/knowledge.js) 的 `QUICK_QUESTIONS` 数组。

### 4. 添加 Agent 工具

见上文第六章「Agent 工具扩展」。

### 5. 重置知识库

删除 `server/knowledge.json`，重启后端会重新写入种子数据。

### 6. 换成其他大模型

编辑 [config/index.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/config/index.js) 的 `baseURL` 和 `apiKey`，OpenAI 兼容接口通用（如 DeepSeek、Moonshot 等）。

---

## 十、常见问题

**Q: 启动报 `EADDRINUSE`？**
A: 3001 端口被占用。用 `lsof -i :3001` 找到进程 kill，或改 `config/index.js` 的 `port`。

**Q: API 调用报 401？**
A: `.env` 中 `DASHSCOPE_API_KEY` 错误或未配置。参考 [.env.example](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/.env.example)。

**Q: `--watch` 没自动重启？**
A: 确认 Node.js 版本 ≥ 18.11（`--watch` 是 18.11+ 特性）。用 `node -v` 检查。

**Q: Agent 一直转圈不返回？**
A: 可能模型陷入工具调用循环。本项目限制 `MAX_ITERATIONS = 8`（[agent.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/agent/agent.service.js)），可调小。也检查工具函数是否正常返回字符串。

**Q: SSE 连接立即断开？**
A: 检查中断监听是否用 `res.on('close')` 而非 `req.on('close')`（见上文第七章）。

**Q: 知识库 JSON 文件在哪？**
A: `server/knowledge.json`（首次启动自动创建，含 embedding 向量）。会话历史在 `server/history.json`。这两个文件已在 `.gitignore` 中（不入库）。

**Q: 向量化失败 / 状态条一直显示"待向量化"？**
A: 检查 `.env` 的 `DASHSCOPE_API_KEY` 是否正确配置。未配置时 `embedding` 置空，语义检索退化为关键词检索。配好后点界面的「补齐向量」或调 `POST /api/knowledge/embeddings/ensure`。

**Q: 换了 embedding 模型后检索不准？**
A: 旧向量是旧模型生成的，新旧维度/语义空间不一致。点界面的「重新生成全部向量」或调 `POST /api/knowledge/embeddings/regenerate` 全量重生成。

**Q: Agent 对概念问题不调检索工具 / 编造"据知识库"？**
A: 这是 qwen-turbo 工具调用不可靠。本项目已用预检索注入规避。若仍出现，确认 [agent.service.js](file:///Users/anyi/Desktop/AIDemos/ai-knowledge-demo/server/src/agent/agent.service.js) 的预检索代码未被注释，且 `shouldForceKbRetrieve` 未误判该查询为问候。

**Q: Node.js 的 `fetch` 报错？**
A: `fetch` 是 Node.js 18+ 内置。若版本较低，需升级 Node 或安装 `node-fetch`。
